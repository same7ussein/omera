import { ConverttonumberPipe } from './converttonumber.pipe';

describe('ConverttonumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverttonumberPipe();
    expect(pipe).toBeTruthy();
  });
});
